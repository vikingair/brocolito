import cac from 'cac';

export const CLI = cac();

// CLI.command('test').option('--foo', 'Foo').action((dir, options) => {
//     console.log({ dir, options });
// });
//
// CLI.help()
//
// CLI.parse();
